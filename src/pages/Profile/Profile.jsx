import React from 'react'

const Profile = () => {
    const suerType = "student"
    if (userType === "student") {
        return (
            <div>Student Profile</div>
        )
    }
  return (
    <div>Profile</div>
  )
}

export default Profile